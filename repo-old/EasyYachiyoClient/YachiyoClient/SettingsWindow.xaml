<Window x:Class="EasyYachiyoClient.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EasyYachiyoClient"
        xmlns:vm="clr-namespace:EasyYachiyoClient.ViewModel"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="设置"
        Height="600"
        Width="800"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Window.DataContext>
        <vm:SettingsViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <DropShadowEffect x:Key="DropShadowEffect"
                          BlurRadius="10"
                          ShadowDepth="3"
                          Opacity="0.3"
                          Direction="270"/>
        
        <!-- 动画资源 -->
        <QuadraticEase x:Key="QuadraticEase" EasingMode="EaseInOut"/>
        <DoubleAnimation x:Key="FadeInAnimation" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
        <DoubleAnimation x:Key="FadeOutAnimation" Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.3"/>
        
        <!-- 设置选项样式 -->
        <Style x:Key="SettingOptionStyle" TargetType="Button">
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="20,20"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="OptionBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter 
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="Center"
                                SnapsToDevicePixels="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E0F0F8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#D0E8F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 圆角矩形控件样式 -->
        <Style x:Key="RoundedRectangleStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        
        <!-- 圆角按钮样式 -->
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0077BE"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter 
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                SnapsToDevicePixels="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005A8C"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#004066"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 圆角下拉框样式 -->
        <Style x:Key="RoundedComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border x:Name="ComboBoxBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <Grid>
                                <ContentPresenter 
                                    Content="{TemplateBinding SelectionBoxItem}"
                                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                    ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                    Margin="{TemplateBinding Padding}"
                                    VerticalAlignment="Center"/>
                                <Popup x:Name="PART_Popup"
                                       AllowsTransparency="True"
                                       IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                       Placement="Bottom"
                                       PopupAnimation="Slide">
                                    <Border x:Name="DropDownBorder"
                                            Background="White"
                                            BorderBrush="#E0E0E0"
                                            BorderThickness="1"
                                            CornerRadius="8"
                                            MinWidth="{TemplateBinding ActualWidth}">
                                        <ScrollViewer x:Name="PART_ScrollViewer">
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                                <ToggleButton x:Name="PART_ToggleButton"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                              Background="Transparent"
                                              BorderBrush="Transparent"
                                              BorderThickness="0"
                                              HorizontalAlignment="Right"
                                              VerticalAlignment="Center"
                                              Margin="5">
                                    <Path Data="M0,0 L8,0 L4,6 Z" Fill="#333333"/>
                                </ToggleButton>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 转换器 -->
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:ProgressToWidthConverter x:Key="ProgressToWidthConverter"/>
        <local:StringEqualsConverter x:Key="StringEqualsConverter"/>
    </Window.Resources>
    <Grid>
        <!-- 淡蓝色10%透明后景 -->
        <Border 
            Background="#19E0F8FF" 
            CornerRadius="12"
            Margin="10"
            Panel.ZIndex="-1"/>
        <Border 
            Background="Transparent"
            CornerRadius="12"
            Effect="{StaticResource DropShadowEffect}"
            Margin="10"
            RenderOptions.BitmapScalingMode="LowQuality"
            RenderOptions.ClearTypeHint="Enabled">
            <Grid>
                <!-- 主内容区域 -->
                <Grid Margin="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="80"/>
                    </Grid.RowDefinitions>
                    
                    <!-- 左侧设置选项栏 -->
                    <Border 
                        Grid.Column="0"
                        Grid.Row="0"
                        Background="#F8F9FA"
                        BorderBrush="#E0E0E0"
                        BorderThickness="1"
                        CornerRadius="8">
                        <StackPanel Margin="10">
                            <Button 
                                Content="模型"
                                Style="{StaticResource SettingOptionStyle}"
                                Command="{Binding SelectSettingOptionCommand}"
                                CommandParameter="Model"
                                Margin="0,10,0,0">
                                <Button.Background>
                                    <MultiBinding Converter="{local:SettingOptionBackgroundConverter}">
                                        <Binding Path="SelectedOption"/>
                                        <Binding Source="Model"/>
                                    </MultiBinding>
                                </Button.Background>
                            </Button>
                            <Button 
                                Content="语言"
                                Style="{StaticResource SettingOptionStyle}"
                                Command="{Binding SelectSettingOptionCommand}"
                                CommandParameter="Language"
                                Margin="0,10,0,0">
                                <Button.Background>
                                    <MultiBinding Converter="{local:SettingOptionBackgroundConverter}">
                                        <Binding Path="SelectedOption"/>
                                        <Binding Source="Language"/>
                                    </MultiBinding>
                                </Button.Background>
                            </Button>
                            <Button 
                                Content="其他"
                                Style="{StaticResource SettingOptionStyle}"
                                Command="{Binding SelectSettingOptionCommand}"
                                CommandParameter="Other"
                                Margin="0,10,0,0">
                                <Button.Background>
                                    <MultiBinding Converter="{local:SettingOptionBackgroundConverter}">
                                        <Binding Path="SelectedOption"/>
                                        <Binding Source="Other"/>
                                    </MultiBinding>
                                </Button.Background>
                            </Button>
                        </StackPanel>
                    </Border>
                    
                    <!-- 右侧设置详情区域 -->
                    <Border 
                        Grid.Column="2"
                        Grid.Row="0"
                        Background="#F8F9FA"
                        BorderBrush="#E0E0E0"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="20">
                        <Grid>
                            <!-- 模型设置 -->
                            <Grid x:Name="ModelSettings" Visibility="{Binding SelectedOption, Converter={local:SettingOptionVisibilityConverter}, ConverterParameter=Model}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="150"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- 模型运行方式 -->
                                <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource RoundedRectangleStyle}" Margin="0,0,10,10">
                                    <TextBlock Text="模型运行方式" FontSize="14" FontWeight="Medium" Margin="10" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource RoundedRectangleStyle}" Margin="0,0,0,10">
                                    <ComboBox Width="120" Style="{StaticResource RoundedComboBoxStyle}" Margin="5" SelectedValue="{Binding ModelRunMode}" SelectedValuePath="Content">
                                        <ComboBoxItem Content="本地"/>
                                        <ComboBoxItem Content="远程"/>
                                    </ComboBox>
                                </Border>
                                
                                <!-- 模型 url 地址 -->
                                <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource RoundedRectangleStyle}" Margin="0,0,10,10">
                                    <TextBlock Text="模型 url 地址" FontSize="14" FontWeight="Medium" Margin="10" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource RoundedRectangleStyle}" Margin="0,0,0,10">
                                    <TextBox Width="120" FontSize="14" Margin="5" Text="{Binding CustomRemoteAddress, Mode=TwoWay}"/>
                                </Border>
                                
                                <!-- 远程环境连接状态 -->
                                <Border Grid.Row="2" Grid.Column="0" Style="{StaticResource RoundedRectangleStyle}" Margin="0,0,10,10">
                                    <TextBlock Text="远程连接状态" FontSize="14" FontWeight="Medium" Margin="10" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="2" Grid.Column="1" Style="{StaticResource RoundedRectangleStyle}" Margin="0,0,0,10">
                                    <TextBlock Text="{Binding RemoteConnectionStatus}" FontSize="14" Margin="10" VerticalAlignment="Center"/>
                                </Border>
                                
                                <!-- 环境检测 -->
                                <Button Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Content="检测环境" Style="{StaticResource RoundedButtonStyle}" Width="150" HorizontalAlignment="Left" Margin="0,0,0,10" Command="{Binding CheckEnvironmentCommand}"/>
                                
                                <!-- 进度条 -->
                                <Border Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource RoundedRectangleStyle}" Margin="0,0,0,10" Visibility="{Binding IsProgressVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <Grid Margin="10">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Text="{Binding ProgressStatus}" FontSize="12" Margin="0,0,0,5"/>
                                        <ProgressBar Grid.Row="1" Value="{Binding ProgressValue}" Height="10" Background="#E0E0E0" Foreground="#0077BE" BorderBrush="Transparent" BorderThickness="0">
                                            <ProgressBar.Template>
                                                <ControlTemplate TargetType="ProgressBar">
                                                    <Grid>
                                                        <Border Background="{TemplateBinding Background}" CornerRadius="5"/>
                                                        <Border Background="{TemplateBinding Foreground}" CornerRadius="5" HorizontalAlignment="Left">
                                                            <Border.Width>
                                                                <MultiBinding Converter="{StaticResource ProgressToWidthConverter}">
                                                                    <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="Value"/>
                                                                    <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="ActualWidth"/>
                                                                </MultiBinding>
                                                            </Border.Width>
                                                        </Border>
                                                    </Grid>
                                                </ControlTemplate>
                                            </ProgressBar.Template>
                                        </ProgressBar>
                                    </Grid>
                                </Border>
                            </Grid>
                            
                            <!-- 语言设置 -->
                            <Grid x:Name="LanguageSettings" Visibility="{Binding SelectedOption, Converter={local:SettingOptionVisibilityConverter}, ConverterParameter=Language}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock 
                                    Grid.Row="0"
                                    Text="语言设置"
                                    FontSize="16"
                                    FontWeight="Medium"
                                    Margin="0,0,0,20"
                                    VerticalAlignment="Center"/>
                                <StackPanel Grid.Row="1">
                                    <Border Style="{StaticResource RoundedRectangleStyle}" Margin="0,0,0,10">
                                        <RadioButton Content="中文" Margin="10" IsChecked="{Binding Language, Converter={StaticResource StringEqualsConverter}, ConverterParameter=中文, Mode=TwoWay}"/>
                                    </Border>
                                    <Border Style="{StaticResource RoundedRectangleStyle}" Margin="0,0,0,10">
                                        <RadioButton Content="English" Margin="10" IsChecked="{Binding Language, Converter={StaticResource StringEqualsConverter}, ConverterParameter=English, Mode=TwoWay}"/>
                                    </Border>
                                </StackPanel>
                            </Grid>
                            
                            <!-- 其他设置 -->
                            <Grid x:Name="OtherSettings" Visibility="{Binding SelectedOption, Converter={local:SettingOptionVisibilityConverter}, ConverterParameter=Other}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock 
                                    Grid.Row="0"
                                    Text="其他设置"
                                    FontSize="16"
                                    FontWeight="Medium"
                                    Margin="0,0,0,20"
                                    VerticalAlignment="Center"/>
                                <StackPanel Grid.Row="1">
                                    <Border Style="{StaticResource RoundedRectangleStyle}" Margin="0,0,0,10">
                                        <CheckBox Content="启动时自动打开上次对话" Margin="10" IsChecked="{Binding AutoOpenLastConversation, Mode=TwoWay}"/>
                                    </Border>
                                    <Border Style="{StaticResource RoundedRectangleStyle}" Margin="0,0,0,10">
                                        <CheckBox Content="启用日志记录" Margin="10" IsChecked="{Binding EnableLogging, Mode=TwoWay}"/>
                                    </Border>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Border>
                    
                    <!-- 关闭按钮 -->
                    <Button 
                        Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="1"
                        Width="40"
                        Height="40"
                        Background="Transparent"
                        BorderBrush="#E74C3C"
                        BorderThickness="1"
                        Foreground="White"
                        Command="{Binding CloseCommand}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Grid x:Name="ButtonGrid">
                                    <Border 
                                        x:Name="ButtonBorder"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="20"
                                        SnapsToDevicePixels="True">
                                        <Border.RenderTransform>
                                            <ScaleTransform ScaleX="1" ScaleY="1"/>
                                        </Border.RenderTransform>
                                        <ContentPresenter 
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                    </Border>
                                    <Ellipse 
                                        x:Name="Ripple"
                                        Fill="White"
                                        Opacity="0"
                                        RenderTransformOrigin="0.5,0.5"
                                        Width="{Binding ActualWidth, ElementName=ButtonGrid}"
                                        Height="{Binding ActualHeight, ElementName=ButtonGrid}">
                                        <Ellipse.RenderTransform>
                                            <ScaleTransform ScaleX="0" ScaleY="0"/>
                                        </Ellipse.RenderTransform>
                                    </Ellipse>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Trigger.EnterActions>
                                            <BeginStoryboard Name="HoverStoryboard">
                                                <Storyboard>
                                                    <!-- 边框颜色变化 -->
                                                    <ColorAnimation 
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                        To="#F39C12"
                                                        Duration="0:0:0.3"/>
                                                    <!-- 缩放效果 -->
                                                    <DoubleAnimation 
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)"
                                                        To="1.02"
                                                        Duration="0:0:0.3"
                                                        EasingFunction="{StaticResource QuadraticEase}"/>
                                                    <DoubleAnimation 
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                                        To="1.02"
                                                        Duration="0:0:0.3"
                                                        EasingFunction="{StaticResource QuadraticEase}"/>
                                                    <!-- 波纹动画 -->
                                                    <DoubleAnimation 
                                                        Storyboard.TargetName="Ripple"
                                                        Storyboard.TargetProperty="Opacity"
                                                        From="0" To="0.3" Duration="0:0:0.1"/>
                                                    <DoubleAnimation 
                                                        Storyboard.TargetName="Ripple"
                                                        Storyboard.TargetProperty="(Ellipse.RenderTransform).(ScaleTransform.ScaleX)"
                                                        From="0" To="2" Duration="0:0:0.8"
                                                        EasingFunction="{StaticResource QuadraticEase}"/>
                                                    <DoubleAnimation 
                                                        Storyboard.TargetName="Ripple"
                                                        Storyboard.TargetProperty="(Ellipse.RenderTransform).(ScaleTransform.ScaleY)"
                                                        From="0" To="2" Duration="0:0:0.8"
                                                        EasingFunction="{StaticResource QuadraticEase}"/>
                                                    <DoubleAnimation 
                                                        Storyboard.TargetName="Ripple"
                                                        Storyboard.TargetProperty="Opacity"
                                                        From="0.3" To="0" Duration="0:0:0.8"
                                                        BeginTime="0:0:0.1"
                                                        EasingFunction="{StaticResource QuadraticEase}"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.EnterActions>
                                        <Trigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <!-- 边框颜色恢复 -->
                                                    <ColorAnimation 
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                        To="#E74C3C"
                                                        Duration="0:0:0.3"/>
                                                    <!-- 缩放恢复 -->
                                                    <DoubleAnimation 
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)"
                                                        To="1"
                                                        Duration="0:0:0.3"
                                                        EasingFunction="{StaticResource QuadraticEase}"/>
                                                    <DoubleAnimation 
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                                        To="1"
                                                        Duration="0:0:0.3"
                                                        EasingFunction="{StaticResource QuadraticEase}"/>
                                                    <!-- 波纹淡出 -->
                                                    <DoubleAnimation 
                                                        Storyboard.TargetName="Ripple"
                                                        Storyboard.TargetProperty="Opacity"
                                                        To="0"
                                                        Duration="0:0:0.3"/>
                                                    <DoubleAnimation 
                                                        Storyboard.TargetName="Ripple"
                                                        Storyboard.TargetProperty="(Ellipse.RenderTransform).(ScaleTransform.ScaleX)"
                                                        To="0"
                                                        Duration="0:0:0.3"/>
                                                    <DoubleAnimation 
                                                        Storyboard.TargetName="Ripple"
                                                        Storyboard.TargetProperty="(Ellipse.RenderTransform).(ScaleTransform.ScaleY)"
                                                        To="0"
                                                        Duration="0:0:0.3"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.ExitActions>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Trigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <!-- 边框颜色变化 -->
                                                    <ColorAnimation 
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                        To="#C0392B"
                                                        Duration="0:0:0.3"/>
                                                    <!-- 缩放效果 -->
                                                    <DoubleAnimation 
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)"
                                                        To="0.98"
                                                        Duration="0:0:0.3"
                                                        EasingFunction="{StaticResource QuadraticEase}"/>
                                                    <DoubleAnimation 
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                                        To="0.98"
                                                        Duration="0:0:0.3"
                                                        EasingFunction="{StaticResource QuadraticEase}"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.EnterActions>
                                        <Trigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <!-- 边框颜色恢复 -->
                                                    <ColorAnimation 
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                        To="#F39C12"
                                                        Duration="0:0:0.3"/>
                                                    <!-- 缩放恢复 -->
                                                    <DoubleAnimation 
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)"
                                                        To="1.02"
                                                        Duration="0:0:0.3"
                                                        EasingFunction="{StaticResource QuadraticEase}"/>
                                                    <DoubleAnimation 
                                                        Storyboard.TargetName="ButtonBorder"
                                                        Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                                        To="1.02"
                                                        Duration="0:0:0.3"
                                                        EasingFunction="{StaticResource QuadraticEase}"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.ExitActions>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <Image 
                            Source="/resource/close.png"
                            Stretch="Uniform"/>
                    </Button>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>